#include <iostream>

using namespace std;

int main() {
    // Creating an integer array named arr of size 10.
    int arr[10];

    // Accessing the element at 0 index and setting its value to 5
    arr[0] = 5;

    // Accessing and printing the value at 0 index
    cout << arr[0];
    return 0;
}